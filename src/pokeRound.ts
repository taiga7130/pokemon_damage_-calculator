// ============================================================
// §2 丸め処理の中核: 4096基準固定小数点（pokeRound）
// ============================================================

/**
 * §2.2 補正適用関数。
 *   floor((value * modifier + 2048) / 4096)
 * +2048 は 0.5 を切り上げる丸め（2048以上を切り上げ）。
 * 全ての乗算補正に統一適用する（壁だけ別丸め等の例外は無い）。
 *
 * @param value    現在のダメージ/ステータス（整数）
 * @param modifier 4096基準の補正値（整数）
 */
export function pokeRound(value: number, modifier: number): number {
  return Math.floor((value * modifier + 2048) / 4096);
}

/** §2.1 倍率の 4096基準 modifier 値。端数倍率は round(倍率 × 4096)。 */
export const MOD = {
  X0_25: 1024, // ×0.25
  X0_5: 2048,  // ×0.5
  X2_3: 2731,  // ×0.66 (2/3)
  X0_75: 3072, // ×0.75
  X1_0: 4096,  // ×1.0
  X1_1: 4506,  // ×1.1
  X1_2: 4915,  // ×1.2
  X1_25: 5120, // ×1.25 (1.25*4096=5120)
  X1_3: 5325,  // ×1.3 (1.3*4096=5324.8 → 5325)
  X1_5: 6144,  // ×1.5
  X2_0: 8192,  // ×2.0
  X2_25: 9216, // ×2.25 (2.25*4096=9216, スナイパー急所)
} as const;
